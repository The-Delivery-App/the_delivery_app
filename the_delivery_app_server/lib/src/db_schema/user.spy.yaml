class: User
table: user
fields:
  # Links to serverpod auth system
  authUserId: int
  
  name: String
  email: String
  phone: String?
  avatarUrl: String?
  
  createdAt: DateTime
  updatedAt: DateTime?

indexes:
  auth_user_idx:
    fields: authUserId
    unique: true